export const tabParams = {
  active: true,
  currentWindow: true
};
export const chromeId = chrome.runtime.id;
export const schemaObj = {
  name: "",
  schema: {
    row: null,
    columns: []
  }
};
