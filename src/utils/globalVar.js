export const tabParams = {
  active: true,
  currentWindow: true
};
export const sTarget = null;
export const prevElementFlag = null;
export const tableItem = [];
export const chromeId = chrome.runtime.id;
